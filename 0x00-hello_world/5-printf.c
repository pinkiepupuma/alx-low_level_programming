#include <stdio.h>

/**
 * main-prints the string with proper grammar
 *
 * Return: if results are false returns zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
